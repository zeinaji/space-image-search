import React from "react";

const SearchResults = (props) => {
  return (
    <>
      <img className="search-image" src={props.image} alt="search-result" />
    </>
  );
};

export default SearchResults;
